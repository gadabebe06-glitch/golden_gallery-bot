services:
  - type: worker
    name: sabian-it-support-bot
    env: python
    runtime: python3.12
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOTBRIDGE_BOT_TOKEN
        sync: false